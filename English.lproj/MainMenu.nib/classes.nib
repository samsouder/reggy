{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {match = id; openPreferencesWindow = id; }; 
            CLASS = ReggyController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                mainWindow = NSWindow; 
                matchAllButton = NSButton; 
                matchCaseButton = NSButton; 
                matchMultiLineButton = NSButton; 
                regexPatternField = NSTextView; 
                statusText = NSTextField; 
                testingStringField = NSTextView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = SSToolbarResizeView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = SSToolbarView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            ACTIONS = {checkForUpdates = id; }; 
            CLASS = SUUpdater; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}